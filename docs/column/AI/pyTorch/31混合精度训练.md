---
date: 2026年06月10日
---

# PyTorch 混合精度训练 (AMP)

混合精度训练是深度学习中最重要的性能优化技术之一。它通过同时使用 FP32（单精度）和 FP16（半精度）浮点数进行计算，可以在几乎不损失模型精度的前提下，显著提升训练速度并减少显存占用。本节详细介绍 PyTorch 中的自动混合精度（Automatic Mixed Precision，AMP）技术。

> **适用版本：**本文代码基于 PyTorch 2.4+ 的`torch.amp.autocast` 和 `torch.amp.GradScaler`  API 编写。PyTorch 1.6+ 使用`torch.cuda.amp`，用法基本一致，文中会标注差异。

------

## 1. 混合精度训练基础

### 1.1 为什么需要混合精度

深度学习模型训练过程中涉及大量的矩阵运算。传统的 FP32（32位浮点）计算精度高，但占用显存大、计算速度慢。FP16（16位浮点）计算速度快、显存占用少，但数值表示范围较小，容易出现梯度下溢（underflow）问题。

混合精度训练的核心理念是：**对精度要求高的操作使用 FP32，对精度要求不高的操作使用 FP16**。这样既能享受 FP16 的速度优势，又能避免精度问题。

下图展示了三种浮点格式的位布局差异——**指数位**决定数值范围，**尾数位**决定精度：

```html
<style>
.float-diagram {
    font-family: system-ui, -apple-system, sans-serif;
    background: #22282a;
    color: #eee;
    padding: 20px;
    /* 取消圆角 */
    border-radius: 0px;
    max-width: 1200px;
}
.float-title {
    text-align: center;
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 15px;
}
.legend {
    display: flex;
    justify-content: flex-end;
    gap: 30px;
    margin-bottom: 20px;
}
.legend-item {
    display: flex;
    align-items: center;
    gap: 8px;
}
.legend-box {
    width: 24px;
    height: 24px;
    /* 取消圆角 */
    border-radius: 0px;
}
.row {
    display: flex;
    align-items: center;
    margin: 15px 0;
    gap: 10px;
}
.label {
    width: 80px;
    font-weight: bold;
    font-size: 20px;
}
.label-small {
    font-size: 14px;
    color: #aaa;
}
.bit-block {
    display: flex;
    flex-grow: 1;
    gap: 8px;
}
.bit {
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: bold;
    /* 取消圆角 */
    border-radius: 0px;
    padding: 12px 8px;
    text-align: center;
}
.s { background: #b33951; width: 50px; }
.exp32 { background: #272a80; flex-grow: 2; }
.man32 { background: #2e8b57; flex-grow: 5; }
.exp16 { background: #b8860b; flex-grow: 1; }
.man16 { background: #8b7500; flex-grow: 2; }
.manbf16 { background: #3cb371; flex-grow: 1.5; }
.warn { background: #8b0000; border: 2px solid #ff6b6b; flex-grow: 1; }
.good { background: #006400; border: 2px solid #50fa7b; flex-grow: 1; }
.note {
    width: 220px;
    font-size: 16px;
    color: #ccc;
}
.footer {
    background: #004d40;
    border: 2px solid #50fa7b;
    color: white;
    padding: 15px;
    /* 取消圆角 */
    border-radius: 0px;
    text-align: center;
    margin-top: 20px;
    font-size: 18px;
}
</style>

<div class="float-diagram">
    <div class="float-title">浮点数格式位布局对比</div>

    <div class="legend">
        <div class="legend-item">
            <div class="legend-box" style="background:#b33951"></div>
            <span>符号位</span>
        </div>
        <div class="legend-item">
            <div class="legend-box" style="background:#272a80"></div>
            <span>指数位</span>
        </div>
        <div class="legend-item">
            <div class="legend-box" style="background:#2e8b57"></div>
            <span>尾数位</span>
        </div>
    </div>

    <!-- FP32 行 -->
    <div class="row">
        <div class="label">FP32<br><span class="label-small">32 bits</span></div>
        <div class="bit-block">
            <div class="bit s">S<br>1</div>
            <div class="bit exp32">Exponent<br>8 bits</div>
            <div class="bit man32">Mantissa<br>23 bits</div>
        </div>
        <div class="note">
            范围: ±3.4×10<sup>38</sup><br>
            精度: ~7 位有效数字
        </div>
    </div>

    <!-- FP16 行 -->
    <div class="row">
        <div class="label" style="color: #f2b749;">FP16<br><span class="label-small">16 bits</span></div>
        <div class="bit-block">
            <div class="bit s">S<br>1</div>
            <div class="bit exp16">Exp<br>5 bits</div>
            <div class="bit man16">Mantissa<br>10 bits</div>
            <div class="bit warn">⚠ 指数位少，易溢出</div>
        </div>
        <div class="note">
            范围: ±65504<br>
            精度: ~3.3 位有效数字
        </div>
    </div>

    <!-- BF16 行 -->
    <div class="row">
        <div class="label" style="color: #50fa7b;">BF16<br><span class="label-small">16 bits</span></div>
        <div class="bit-block">
            <div class="bit s">S<br>1</div>
            <div class="bit exp32">Exponent<br>8 bits（同 FP32）</div>
            <div class="bit manbf16">Mantissa<br>7 bits</div>
            <div class="bit good">✓ 范围与 FP32 相同</div>
        </div>
        <div class="note">
            范围: ±3.4×10<sup>38</sup><br>
            精度: ~2.4 位有效数字
        </div>
    </div>

    <div class="footer">
        BF16 保留了 FP32 的指数位 → 数值范围相同 → 训练更稳定，通常无需 GradScaler
    </div>
</div>
```

### 1.2 混合精度的优势

| 指标     | 提升效果    | 说明                             |
| :------- | :---------- | :------------------------------- |
| 训练速度 | 提升 2-3 倍 | 依赖 GPU Tensor Core 支持        |
| 显存占用 | 减少约 50%  | 激活值和中间结果以 FP16 存储     |
| 内存带宽 | 减少约 50%  | 更小的数据体积意味着更少的传输量 |
| 通信开销 | 减少约 50%  | 分布式训练中梯度传输量减半       |

### 1.3 Tensor Core 加速原理

NVIDIA 的 Tensor Core 是一种专门用于矩阵运算的硬件单元。它可以在一个时钟周期内完成 4×4 矩阵乘加运算（D = A × B + C），这就是 FP16 训练加速的主要来源。相比普通 CUDA 核心需要多次指令才能完成同样的运算，Tensor Core 将其压缩为单条指令。

支持 Tensor Core 的 GPU 包括：

- **Volta 架构**（V100）— 首代 Tensor Core，仅支持 FP16
- **Turing 架构**（RTX 20 系）— 支持 FP16 / INT8 / INT4
- **Ampere 架构**（RTX 30 系、A100）— 新增 BF16 / TF32 支持
- **Ada Lovelace 架构**（RTX 40 系）— 新增 FP8 支持
- **Hopper 架构**（H100）— 新增 FP8 Transformer Engine

> 消费级 RTX GPU 同样支持 Tensor Core，如 RTX 3060 及以上型号均可享受 AMP 加速。

------

## 2. PyTorch AMP 基础用法

### 2.1 autocast 与 GradScaler

PyTorch 的 AMP API 主要包含两个核心组件：

- `autocast`：上下文管理器，自动将区域内的运算切换为 FP16（对精度敏感的操作会自动回退到 FP32）
- `GradScaler`：动态调整梯度缩放系数，放大 FP16 梯度以防止下溢（仅 FP16 需要，BF16 通常不需要）

下图展示了 AMP 训练一个完整 step 的数据流：

```html
<style>
.amp-diagram {
    font-family: system-ui, -apple-system, sans-serif;
    background: #222;
    color: #eee;
    padding: 20px;
    border-radius: 0px;
    max-width: 100%;
}
.amp-title {
    text-align: center;
    font-size: 28px;
    font-weight: bold;
    margin-bottom: 30px;
}
.flow-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 30px;
    gap: 12px;
}
.flow-item {
    flex: 1;
    text-align: center;
    padding: 20px 10px;
    border: 2px solid #aaa;
    border-radius: 0px;
    background: transparent;
}
.flow-item.green {
    background: #004d40;
    border-color: #50fa7b;
    color: #50fa7b;
}
.flow-item.orange {
    background: #8b6914;
    border-color: #f39c12;
    color: #f39c12;
}
.flow-arrow {
    color: #aaa;
    font-size: 24px;
    font-weight: bold;
}
.legend-row {
    display: flex;
    gap: 20px;
    margin-bottom: 20px;
}
.legend-item {
    flex: 1;
    padding: 15px;
    border-radius: 0px;
    text-align: center;
    font-size: 20px;
    font-weight: bold;
}
.legend-item.green {
    background: #004d40;
    color: #50fa7b;
}
.legend-item.orange {
    background: #8b6914;
    color: #f39c12;
}
.legend-item.blue {
    background: #1a237e;
    color: #64b5f6;
}
.footer-text {
    text-align: center;
    font-size: 20px;
    color: #aaa;
    margin-top: 20px;
}
</style>

<div class="amp-diagram">
    <div class="amp-title">AMP 单步训练流程</div>

    <div class="flow-row">
        <div class="flow-item">
            <div style="font-size: 24px; font-weight: bold;">输入数据</div>
            <div style="font-size: 18px; color: #aaa; margin-top: 8px;">FP32</div>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-item green">
            <div style="font-size: 24px; font-weight: bold;">autocast<br>前向传播</div>
            <div style="font-size: 18px; margin-top: 8px;">自动选择 FP16/FP32</div>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-item">
            <div style="font-size: 24px; font-weight: bold;">计算 Loss</div>
            <div style="font-size: 18px; color: #aaa; margin-top: 8px;">FP32</div>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-item orange">
            <div style="font-size: 24px; font-weight: bold;">GradScaler<br>缩放 + 反向传播</div>
            <div style="font-size: 18px; margin-top: 8px;">loss × scale_factor</div>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-item">
            <div style="font-size: 24px; font-weight: bold;">Unscale 梯度</div>
            <div style="font-size: 18px; color: #aaa; margin-top: 8px;">+ 梯度裁剪<br>grad / scale_factor</div>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-item">
            <div style="font-size: 24px; font-weight: bold;">优化器更新</div>
            <div style="font-size: 18px; color: #aaa; margin-top: 8px;">FP32 权重</div>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-item orange">
            <div style="font-size: 24px; font-weight: bold;">更新<br>Scaler</div>
            <div style="font-size: 18px; margin-top: 8px;">调整 scale</div>
        </div>
    </div>

    <div style="border: 2px solid #aaa; border-radius: 0px; padding: 20px;">
        <div class="legend-row">
            <div class="legend-item green">autocast 控制 — 自动选择精度</div>
            <div class="legend-item orange">GradScaler 控制 — 防止梯度下溢</div>
            <div class="legend-item blue">FP32 权重更新 — 保持精度</div>
        </div>
        <div class="footer-text">关键：权重始终以 FP32 存储和更新，仅在计算时临时转换为 FP16</div>
    </div>
</div>
```

### 2.2 基础使用示例

```py
import torch
import torch.nn as nn
import torch.optim as optim
# ── 更新点：从 torch.amp 导入 ──────────────────────────
from torch.amp import autocast, GradScaler

# 检查 CUDA 是否可用
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print(f"使用设备: {device}")

if torch.cuda.is_available():
    print(f"GPU: {torch.cuda.get_device_name(0)}")
    print(f"支持 BF16: {torch.cuda.is_bf16_supported()}")

# ── 模型定义 ──────────────────────────────────────
class SimpleModel(nn.Module):
    def __init__(self):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(128, 256),
            nn.ReLU(),
            nn.Linear(256, 256),
            nn.ReLU(),
            nn.Linear(256, 10)
        )

    def forward(self, x):
        return self.net(x)

model = SimpleModel().to(device)
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=1e-3)

# ── 关键变更：GradScaler 现在也是从 torch.amp 导入 ──
scaler = GradScaler(device='cuda')

# 训练循环
def train_epoch_amp(model, loader, criterion, optimizer, scaler, device):
    model.train()
    total_loss = 0
    correct = 0
    total = 0

    for inputs, labels in loader:
        inputs = inputs.to(device, non_blocking=True)
        labels = labels.to(device, non_blocking=True)

        optimizer.zero_grad()

        # ── 核心：autocast 现在直接使用 torch.amp 模块 ─────
        # device_type 明确指定为 'cuda'
        with autocast(device_type='cuda', dtype=torch.float16):
            outputs = model(inputs)
            loss = criterion(outputs, labels)

        # ── 使用 scaler 进行反向传播 ─────────────
        scaler.scale(loss).backward()
        scaler.step(optimizer)
        scaler.update()

        # 统计
        total_loss += loss.item() * inputs.size(0)
        _, predicted = outputs.max(1)
        correct += predicted.eq(labels).sum().item()
        total += labels.size(0)

    return total_loss / total, correct / total

# 模拟数据
train_loader = [
    (torch.randn(32, 128), torch.randint(0, 10, (32,))) for _ in range(10)
]

# 开始训练
for epoch in range(3):
    loss, acc = train_epoch_amp(
        model, train_loader, criterion, optimizer, scaler, device
    )
    print(f"Epoch {epoch+1}: Loss={loss:.4f}, Acc={acc:.4f}")

print("PyTorch 2.4+ 混合精度训练完成！")
```

### 2.3 BF16 与 FP16 的选择

现代 GPU 支持两种半精度格式，它们的核心区别在于指数位和尾数位的分配策略不同：

| 格式 | 指数位 | 尾数位  | 数值范围  | 精度           | 适用场景                        |
| :--- | :----- | :------ | :-------- | :------------- | :------------------------------ |
| FP16 | 5 bits | 10 bits | ±65504    | 较高（~3.3位） | 需要兼容旧 GPU（V100/RTX 20系） |
| BF16 | 8 bits | 7 bits  | ±3.4×10³⁸ | 较低（~2.4位） | 追求稳定性（A100/RTX 30系+）    |

选择建议：如果硬件支持 BF16，**优先使用 BF16**。它的数值范围与 FP32 完全相同，训练过程中几乎不会出现溢出问题，且不需要 GradScaler。

```py
import torch
# ── 更新点：从 torch.amp 导入 autocast ──────────────────
from torch.amp import autocast

# 检查硬件支持
device = 'cuda'
print(f"硬件支持 BF16: {torch.cuda.is_bf16_supported()}")

# ── BF16 训练循环 ──────────────────────────────────────
# 注意：BF16 不需要 GradScaler，代码更简洁
model.train()

for inputs, labels in train_loader:
    inputs = inputs.to(device)
    labels = labels.to(device)
    
    optimizer.zero_grad()

    # 核心：使用 autocast 指定 dtype 为 torch.bfloat16
    # device_type 必须明确为 'cuda' (或其他支持的设备类型)
    with autocast(device_type='cuda', dtype=torch.bfloat16):
        outputs = model(inputs)
        loss = criterion(outputs, labels)

    # 直接进行反向传播，不需要 loss.backward() 之外的额外处理
    loss.backward()
    optimizer.step()

print("PyTorch 2.4+ BF16 混合精度训练完成！")
```

## 3. 进阶技巧与优化

### 3.1 动态损失缩放

GradScaler 的核心机制是**动态损失缩放**——它会根据训练状态自动调整缩放因子，既能防止梯度下溢，又能在训练稳定时逐步降低缩放以减少精度损失：

```html
<style>
.dls-diagram {
    font-family: system-ui, -apple-system, sans-serif;
    background: #222;
    color: #eee;
    padding: 20px;
    max-width: 1000px;
    margin: 0 auto;
}
.dls-title {
    text-align: center;
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 30px;
}
.dls-container {
    position: relative;
    width: 100%;
    height: 500px;
}
/* 节点样式 - 无圆角 */
.node {
    position: absolute;
    padding: 15px 25px;
    text-align: center;
    font-weight: bold;
    border: 2px solid;
}
.node-blue {
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    background: #1a237e;
    border-color: #1a237e;
    color: #fff;
    width: 180px;
}
.node-gray {
    top: 100px;
    left: 50%;
    transform: translateX(-50%);
    background: transparent;
    border-color: #aaa;
    color: #ddd;
    width: 200px;
}
.node-decision {
    top: 200px;
    left: 50%;
    transform: translateX(-50%);
    background: #8b6914;
    border-color: #f39c12;
    color: #fff;
    width: 220px;
    /* 菱形节点 */
    clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
    padding: 30px 20px;
}
.node-red {
    top: 200px;
    left: 50px;
    background: #8b0000;
    border-color: #ff6b6b;
    color: #fff;
    width: 180px;
}
.node-green {
    top: 200px;
    right: 50px;
    background: #004d40;
    border-color: #50fa7b;
    color: #50fa7b;
    width: 220px;
}
.node-gray2 {
    top: 320px;
    right: 50px;
    background: transparent;
    border-color: #aaa;
    color: #ddd;
    width: 240px;
}
.node-green2 {
    top: 420px;
    right: 50px;
    background: #004d40;
    border-color: #50fa7b;
    color: #50fa7b;
    width: 180px;
}
.node-footer {
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    background: transparent;
    border-color: #aaa;
    color: #aaa;
    width: 600px;
}
/* 箭头样式 */
.arrow {
    position: absolute;
    border: 2px solid;
}
.arrow-solid {
    border-style: solid;
}
.arrow-dashed {
    border-style: dashed;
}
/* 节点文字 */
.node small {
    font-size: 14px;
    display: block;
    margin-top: 5px;
    opacity: 0.8;
}
/* 箭头标签 */
.arrow-label {
    position: absolute;
    font-weight: bold;
}
</style>

<div class="dls-diagram">
    <div class="dls-title">动态损失缩放（Dynamic Loss Scaling）</div>

    <div class="dls-container">
        <!-- 节点 -->
        <div class="node node-blue">Scale × Loss</div>
        <div class="node node-gray">反向传播</div>
        <div class="node node-decision">梯度中存在<br>Inf / NaN?</div>
        <div class="node node-red">缩小 scale<br>× backoff<br><small>（默认 0.5）</small></div>
        <div class="node node-green">Unscale + 更新参数<br><small>连续成功 +1</small></div>
        <div class="node node-gray2">连续成功 ≥ growth_interval?<br><small>（默认 2000 步）</small></div>
        <div class="node node-green2">增大 scale × growth</div>
        <div class="node node-footer">growth_factor 默认 2.0 · backoff_factor 默认 0.5 · 动态平衡精度与稳定</div>

        <!-- 箭头（文字描述位置，实际渲染可配合编辑器的 flex/grid 或直接在 Obsidian 中使用，这里仅还原节点结构） -->
        <!-- 说明：Obsidian 中纯 HTML 无法完美绘制斜线箭头，但节点布局和配色已完全还原，如需箭头可配合 CSS 伪元素实现 -->
    </div>
</div>
```

GradScaler 自动管理上述反馈循环，你只需通过参数微调其行为：

```py
from torch.amp import GradScaler

# ── 2.4+ 推荐写法 ──────────────────────────────────────────
# 虽然参数名不变，但现在由 torch.amp 模块管理
scaler = GradScaler(
    init_scale=2**16,      
    growth_factor=2.0,     
    backoff_factor=0.5,    
    growth_interval=2000,  
    enabled=True           
)

# ── 工作流程查看 ──────────────────────────────────────────
# 获取当前 scale
print(f"当前缩放因子: {scaler.get_scale()}")

# 在 2.4+ 中，检查是否检测到 Inf/NaN 的推荐方法
# 使用 scaler.is_enabled() 确保功能开启
if scaler.is_enabled():
    # scaler._found_inf 是一个内部张量，用于记录当前 step 的状态
    # 在生产代码中，建议通过 scaler.unscale_ 后的检查逻辑来判断
    found_inf = scaler._found_inf.item() if scaler._found_inf is not None else 0
    print(f"最近是否检测到溢出 (Inf/NaN): {bool(found_inf)}")
```

### 3.2 梯度累积中的 AMP

梯度累积用于在有限显存下模拟更大的 batch size。使用 AMP 时，需要注意每个子 batch 的 loss 必须除以累积步数，否则最终梯度会被放大：

```py
from torch.amp import autocast, GradScaler

accumulation_steps = 8
scaler = GradScaler(device='cuda')
model.train()
optimizer.zero_grad() # 在循环外初始化

for batch_idx, (inputs, labels) in enumerate(train_loader):
    inputs, labels = inputs.to(device, non_blocking=True), labels.to(device, non_blocking=True)

    with autocast(device_type='cuda', dtype=torch.float16):
        outputs = model(inputs)
        loss = criterion(outputs, labels) / accumulation_steps

    scaler.scale(loss).backward()

    # 每 accumulation_steps 更新一次
    if (batch_idx + 1) % accumulation_steps == 0:
        scaler.step(optimizer)
        scaler.update()
        optimizer.zero_grad()

# ── 更优雅的处理末尾梯度 ──────────────────────────
# 如果循环结束时 (batch_idx + 1) % accumulation_steps != 0，
# 且你非常在意那部分不完整的梯度带来的权重偏差，
# 建议在循环后手动执行一次更新（取决于你的业务逻辑对精度的要求）。
# 如果不在意，通常直接丢弃或忽略即可。
```

### 3.3 验证与推理中的 AMP

验证和推理阶段同样推荐使用 AMP。由于不需要反向传播和梯度缩放，代码更加简洁：

```py
import torch
from torch.amp import autocast

# ── 2.4+ 推理最佳实践 ─────────────────────────────────
# 1. 使用 @torch.inference_mode() 代替 @torch.no_grad()
#    它不仅禁用梯度计算，还禁用了版本计数器等内部开销，运行更快且更省内存。
@torch.inference_mode()
def inference_fast(model, inputs, device):
    model.eval()
    # 2. 推理时使用 autocast，dtype 默认 float16
    #    对于支持 BF16 的硬件（如 A100/H100），可以指定 dtype=torch.bfloat16
    with autocast(device_type='cuda', dtype=torch.float16):
        outputs = model(inputs.to(device))
    return outputs

# ── 高效批量推理 ──────────────────────────────────────
def batch_inference(model, dataloader, device):
    model.eval()
    all_outputs = []
    
    # 作用域外层使用 inference_mode
    with torch.inference_mode():
        for inputs in dataloader:
            # 同样在循环内使用 autocast
            with autocast(device_type='cuda', dtype=torch.float16):
                outputs = model(inputs.to(device))
                all_outputs.append(outputs.cpu())
                
    return torch.cat(all_outputs, dim=0)
```

## 4. 常见问题与解决方案

### 4.1 训练不稳定

| 问题                | 原因                                        | 解决方案                                        |
| :------------------ | :------------------------------------------ | :---------------------------------------------- |
| Loss 变为 NaN / Inf | 梯度溢出（scale 过大或学习率过高）          | 降低 `init_scale`，添加梯度裁剪，降低学习率     |
| Loss 不下降 / 震荡  | 梯度下溢（scale 过小，梯度被截断为 0）      | 提高 `init_scale`，检查梯度统计，尝试 BF16      |
| 精度下降明显        | 某些层（如 LayerNorm、Softmax）对 FP16 敏感 | 手动将这些层保持 FP32（见 4.2）                 |
| 训练后期不稳定      | 模型参数值域变化导致 scale 不匹配           | 适当增大 `growth_interval`，使 scale 调整更保守 |

### 4.2 手动控制精度

某些操作在 FP16 下精度损失较大，需要手动指定使用 FP32。PyTorch 的 autocast 已经内置了对这些操作的保护，但自定义操作可能需要手动处理：

```py
import torch
import torch.nn as nn
# 统一从 torch.amp 导入
from torch.amp import autocast

# ── 方法一：包裹一个强制 FP32 的损失函数 ────────
class FP32Loss(nn.Module):
    """强制在 FP32 下计算的损失函数包装器"""
    def __init__(self, base_criterion):
        super().__init__()
        self.base_criterion = base_criterion

    def forward(self, input, target):
        # 显式转换为 FP32，脱离 autocast 的影响
        return self.base_criterion(input.float(), target.float())


# ── 方法二：在 autocast 中局部禁用 ──────────────
# 2.4+ 推荐增加 device_type 参数
with autocast(device_type='cuda'):
    outputs = model(inputs)

    # 对精度敏感的操作，临时禁用 autocast
    with autocast(device_type='cuda', enabled=False):
        loss = criterion(outputs.float(), labels)


# ── 方法三：模型中特定层保持 FP32 ──────────────
class CustomModel(nn.Module):
    def __init__(self):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 64, 3, padding=1),
            nn.BatchNorm2d(64),  # BN 对精度敏感
            nn.ReLU(),
            nn.Conv2d(64, 128, 3, padding=1),
            nn.BatchNorm2d(128),
            nn.ReLU(),
        )
        self.classifier = nn.Linear(128, 10)

    def forward(self, x):
        x = self.features[0](x)           # Conv: FP16（由 autocast 控制）
        x = self.features[1](x.float())   # BN: 强制 FP32
        x = self.features[2](x)           # ReLU: FP16
        x = self.features[3](x)
        x = self.features[4](x.float())   # BN: 强制 FP32
        x = self.features[5](x)
        x = x.mean(dim=[2, 3])            # Global Average Pooling
        x = self.classifier(x)
        return x
```

> **提示：**PyTorch 的 autocast 会自动将以下操作保持在 FP32：softmax、log_softmax、cross_entropy、layer_norm、batch_norm 等。通常只有自定义算子才需要手动处理。

### 4.3 梯度裁剪与 AMP

梯度裁剪必须在 `scaler.step()` 之前、且在 `scaler.unscale_()` 之后执行。这是因为裁剪需要作用在**真实梯度值**上，而非缩放后的梯度：

```py
from torch.amp import autocast, GradScaler
import torch.nn as nn

# 初始化 2.4+ 的 Scaler
scaler = GradScaler(device='cuda')

# ── 正确的梯度裁剪顺序 ──────────────────────────
for inputs, labels in train_loader:
    inputs, labels = inputs.to(device), labels.to(device)
    optimizer.zero_grad()

    with autocast(device_type='cuda', dtype=torch.float16):
        outputs = model(inputs)
        loss = criterion(outputs, labels)

    # Step 1: 缩放 loss 并反向传播
    scaler.scale(loss).backward()

    # Step 2: 反缩放梯度（将梯度从 scale 还原到真实值）
    # 必须在裁剪和 step 之前调用
    scaler.unscale_(optimizer)

    # Step 3: 对真实梯度进行裁剪
    nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)

    # Step 4: 更新参数（内部会检查梯度是否含 Inf/NaN）
    scaler.step(optimizer)

    # Step 5: 更新缩放因子
    scaler.update()
```

## 5. 性能对比与最佳实践

### 5.1 性能基准测试

```py
import time
import torch
import torch.nn as nn
import torch.optim as optim
# ── 更新点：从 torch.amp 导入核心组件 ────────────
from torch.amp import autocast, GradScaler

def benchmark_training(model, train_loader, device, use_amp=True,
                       dtype=torch.float16, num_iterations=100):
    """对比 FP32 与 AMP 的训练速度"""
    model = model.to(device)
    criterion = nn.CrossEntropyLoss()
    optimizer = optim.Adam(model.parameters(), lr=1e-3)
    
    # ── 2.4+ 写法：明确指定 device ───────────────
    scaler = GradScaler(device='cuda', enabled=use_amp and dtype == torch.float16)

    # ── 预热（前 10 步不计时）────────────────────
    for i, (inputs, labels) in enumerate(train_loader):
        if i >= 10: break
        inputs, labels = inputs.to(device, non_blocking=True), labels.to(device, non_blocking=True)
        optimizer.zero_grad()

        if use_amp:
            with autocast(device_type='cuda', dtype=dtype):
                outputs = model(inputs)
                loss = criterion(outputs, labels)
            scaler.scale(loss).backward()
            scaler.step(optimizer)
            scaler.update()
        else:
            outputs = model(inputs)
            loss = criterion(outputs, labels)
            loss.backward()
            optimizer.step()

    # ── 正式测试 ─────────────────────────────────
    torch.cuda.synchronize()
    start = time.time()

    for i, (inputs, labels) in enumerate(train_loader):
        if i >= num_iterations: break
        inputs, labels = inputs.to(device, non_blocking=True), labels.to(device, non_blocking=True)
        optimizer.zero_grad()

        if use_amp:
            with autocast(device_type='cuda', dtype=dtype):
                outputs = model(inputs)
                loss = criterion(outputs, labels)
            scaler.scale(loss).backward()
            scaler.step(optimizer)
            scaler.update()
        else:
            outputs = model(inputs)
            loss = criterion(outputs, labels)
            loss.backward()
            optimizer.step()

    torch.cuda.synchronize()
    elapsed = time.time() - start

    return elapsed / num_iterations

# ── 运行对比 ─────────────────────────────────────
model = SimpleModel() # 假设 SimpleModel 已定义

fp32_time = benchmark_training(model, train_loader, device, use_amp=False)
fp16_time = benchmark_training(model, train_loader, device, use_amp=True, dtype=torch.float16)

print(f"FP32 平均耗时: {fp32_time*1000:.2f} ms/batch")
print(f"FP16 平均耗时: {fp16_time*1000:.2f} ms/batch")
print(f"加速比: {fp32_time / fp16_time:.2f}x")

if torch.cuda.is_bf16_supported():
    bf16_time = benchmark_training(
        model, train_loader, device, use_amp=True, dtype=torch.bfloat16
    )
    print(f"BF16 平均耗时: {bf16_time*1000:.2f} ms/batch")
    print(f"BF16 加速比: {fp32_time / bf16_time:.2f}x")
```

### 5.2 显存对比

```py
import torch
import torch.nn as nn
import torch.optim as optim
# ── 更新点：从 torch.amp 统一导入 ───────────────
from torch.amp import autocast, GradScaler

def compare_memory(model_class, train_loader, device):
    """对比 FP32 与 AMP 的显存占用"""
    def reset():
        torch.cuda.empty_cache()
        torch.cuda.reset_peak_memory_stats()

    # ── FP32 显存测试 ────────────────────────────
    reset()
    model_fp32 = model_class().to(device)
    optimizer_fp32 = optim.Adam(model_fp32.parameters())

    for inputs, labels in list(train_loader)[:5]:
        inputs, labels = inputs.to(device, non_blocking=True), labels.to(device, non_blocking=True)
        optimizer_fp32.zero_grad()
        outputs = model_fp32(inputs)
        loss = nn.CrossEntropyLoss()(outputs, labels)
        loss.backward()
        optimizer_fp32.step()

    fp32_peak = torch.cuda.max_memory_allocated(device) / 1024**2

    # ── AMP 显存测试 ─────────────────────────────
    reset()
    model_amp = model_class().to(device)
    optimizer_amp = optim.Adam(model_amp.parameters())
    # 2.4+ 推荐：初始化时明确指定 device
    scaler = GradScaler(device='cuda')

    for inputs, labels in list(train_loader)[:5]:
        inputs, labels = inputs.to(device, non_blocking=True), labels.to(device, non_blocking=True)
        optimizer_amp.zero_grad()
        with autocast(device_type='cuda', dtype=torch.float16):
            outputs = model_amp(inputs)
            loss = nn.CrossEntropyLoss()(outputs, labels)
        
        scaler.scale(loss).backward()
        scaler.step(optimizer_amp)
        scaler.update()

    amp_peak = torch.cuda.max_memory_allocated(device) / 1024**2

    print(f"FP32 峰值显存: {fp32_peak:.1f} MB")
    print(f"AMP  峰值显存: {amp_peak:.1f} MB")
    print(f"显存节省: {(fp32_peak - amp_peak) / fp32_peak * 100:.1f}%")

# 调用对比函数
compare_memory(SimpleModel, train_loader, device)
```

### .3 最佳实践总结

| 场景                    | 推荐配置                    | 原因                                |
| :---------------------- | :-------------------------- | :---------------------------------- |
| A100 / H100 / RTX 40 系 | BF16，不需要 GradScaler     | 数值范围与 FP32 相同，最稳定        |
| V100 / RTX 20 系        | FP16 + GradScaler           | 硬件不支持 BF16，需要 scaler 防溢出 |
| RTX 30 系               | BF16 优先，FP16 备选        | Ampere 架构支持 BF16                |
| 大模型训练（显存紧张）  | AMP + 梯度累积 + 梯度检查点 | 三者叠加可最大化显存利用率          |
| 推理部署                | autocast + inference_mode   | 无需 scaler，推理最快               |

> PyTorch 2.0+ 已将 AMP 集成到 `torch.compile` 中，可以自动应用混合精度优化。使用 `torch.compile(model)` 时，编译器会自动判断哪些操作适合 FP16。

------

## 6. 与其他优化技术的结合

### 6.1 AMP + torch.compile

```py
import torch
import torch.nn as nn
import torch.optim as optim
# ── 更新点：统一从 torch.amp 导入 ───────────────
from torch.amp import autocast, GradScaler

# ── 方式一：编译模型 (torch.compile) ──────────
model = model.to(device)
# torch.compile 是 2.0+ 的杀手级特性，推荐在训练循环前调用
model_compiled = torch.compile(model, mode="reduce-overhead")

scaler = GradScaler(device='cuda')

# ── 方式二：启用 TF32 加速 (Ampere+ 架构首选) ──
# TF32 在 Tensor Core 上提供接近 FP16 的速度，同时保持 FP32 的动态范围
torch.backends.cuda.matmul.allow_tf32 = True
torch.backends.cudnn.allow_tf32 = True
torch.backends.cudnn.benchmark = True  # 对卷积网络性能有显著提升

# 训练循环
for inputs, labels in train_loader:
    inputs, labels = inputs.to(device, non_blocking=True), labels.to(device, non_blocking=True)
    optimizer.zero_grad()

    # AMP 依然是混合精度的基础，与 compile 完美兼容
    with autocast(device_type='cuda', dtype=torch.float16):
        outputs = model_compiled(inputs)
        loss = criterion(outputs, labels)

    scaler.scale(loss).backward()
    scaler.step(optimizer)
    scaler.update()
```

### 6.2 AMP + 分布式训练

```py
import torch
import torch.nn as nn
import torch.optim as optim
import torch.distributed as dist
from torch.nn.parallel import DistributedDataParallel as DDP
# ── 更新点：从 torch.amp 统一导入 ───────────────
from torch.amp import autocast, GradScaler

def setup(rank, world_size):
    dist.init_process_group("nccl", rank=rank, world_size=world_size)
    torch.cuda.set_device(rank)

def train_ddp_amp(rank, world_size, train_loader, criterion):
    setup(rank, world_size)
    device = torch.device(f"cuda:{rank}")

    model = SimpleModel().to(device)
    # DDP 包装
    model = DDP(model, device_ids=[rank])

    optimizer = optim.Adam(model.parameters(), lr=1e-3)
    # ── 2.4+ 推荐：明确指定设备 ──────────────────
    scaler = GradScaler(device='cuda')

    for inputs, labels in train_loader:
        inputs = inputs.to(device, non_blocking=True)
        labels = labels.to(device, non_blocking=True)

        optimizer.zero_grad()

        with autocast(device_type='cuda', dtype=torch.float16):
            outputs = model(inputs)
            loss = criterion(outputs, labels)

        # 1. 缩放 loss 并反向传播
        # DDP 会自动在 backward 过程中同步所有进程间的梯度
        scaler.scale(loss).backward()

        # 2. 反缩放梯度（必须在梯度裁剪前）
        # DDP 内部已处理完梯度同步，unscale_ 对所有进程的梯度进行一致性处理
        scaler.unscale_(optimizer)

        # 3. 梯度裁剪
        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)

        # 4. 更新参数
        scaler.step(optimizer)
        scaler.update()

    dist.destroy_process_group()
```

## 总结

混合精度训练是当前深度学习训练的**标准实践**。

核心要点：

- **选择精度格式**：硬件支持 BF16 时优先使用，否则使用 FP16 + GradScaler
- **理解 autocast**：它自动管理精度切换，大多数情况下无需手动干预
- **理解 GradScaler**：仅 FP16 需要，通过动态缩放防止梯度下溢
- **注意裁剪顺序**：unscale → clip → step → update，顺序不可颠倒
- **善用组合优化**：AMP 可与 torch.compile、梯度累积、分布式训练无缝结合
